<form method="post" action="{{ path('veille_informatique_delete', {'id': veille_informatique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veille_informatique.id) }}">
    <button class="btn">Delete</button>
</form>
